# Copy to .env and fill in real values. NEVER commit .env.
# MCP servers in .agents/mcp/servers.json read credentials from here through the
# environment; no secret belongs in a committed adapter.

# Optional. Context7 works without it at a lower rate limit.
# Free key: https://context7.com/dashboard
CONTEXT7_API_KEY=
