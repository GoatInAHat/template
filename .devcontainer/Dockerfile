# The base image lives here rather than in devcontainer.json so a project can
# swap in the stack it actually needs, and so Dependabot can keep it current.
FROM mcr.microsoft.com/devcontainers/base:ubuntu
