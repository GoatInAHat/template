Follow [AGENTS.md](../AGENTS.md). It is the canonical instruction file for every
agent working in this repository.

Path-scoped guidance belongs in `.github/instructions/*.instructions.md` with an
`applyTo` glob — that format is Copilot-specific and is not shared with other
agents. Anything every agent needs goes in `AGENTS.md` instead.
